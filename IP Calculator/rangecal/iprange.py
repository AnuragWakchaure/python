from brcal import broad as a
from netcal import nwadd as n

def iprange(uservar,uservar1,uservar2,uservar3):
    print(f"usable range  is {uservar}.{uservar1}.{uservar2}.1 to {uservar}.{uservar1}.{uservar2}.{uservar3}")
     